package br.com.ufpb.classes;

import java.util.ArrayList;

import javax.swing.JOptionPane;

public class AlunoMetodos {
	
	ArrayList<Aluno> alunos;
	
	public AlunoMetodos() {
		alunos = new ArrayList<>();
	}
	
	public void cadastrarAluno(String nome, String matricula){
		alunos.add(new Aluno(nome, matricula));
	}
	
	public void listarAluno(){
		if(alunos.size() > 0){
			JOptionPane.showMessageDialog(null, alunos.size() + " alunos cadastrados!");
			for (Aluno aluno : alunos) {
				JOptionPane.showMessageDialog(null, "Nome: " + aluno.getNome() + "\nMatrícula: " + aluno.getMatricula());
			}
		}else{
			JOptionPane.showMessageDialog(null, "Nenhum aluno cadastrado!");
		}
	}
	
	public void excluirAluno(){
		if(alunos.size() > 0){
			for (Aluno aluno : alunos) {
				String opcao = JOptionPane.showInputDialog("Nome: " + aluno.getNome() + "\nMatrícula: " + aluno.getMatricula() + "\n\nExcluir?\n1 - Sim\nQualquer valor - Não");
				if(opcao.equals("1")){
					alunos.remove(aluno);
				}
			}
		}else{
			JOptionPane.showMessageDialog(null, "Nenhum aluno cadastrado!");
		}
	}
}
